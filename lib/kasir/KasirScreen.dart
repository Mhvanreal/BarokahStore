import 'package:flutter/material.dart';

class KasirPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Halaman Kasir")),
      body: Center(child: Text("Ini adalah halaman Kasir")),
    );
  }
}
